const boton = document.querySelector("#boton");

const tamañoPantalla = () => {
    const v_width = window.screen.width;
    alert(`Èl ancho de la pantalla es: ${v_width}`);
}

// boton.addEventListener("click",tamañoPantalla);